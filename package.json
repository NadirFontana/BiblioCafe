{
  "name": "bibliocafe-menu-neon",
  "version": "1.0.0",
  "description": "BiblioCafè Menu API with Neon PostgreSQL",
  "type": "module",
  "scripts": {
    "setup-db": "node scripts/setup-database.js",
    "verify-db": "node scripts/verify-database.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
